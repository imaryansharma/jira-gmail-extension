{
  "name": "jira-gmail-tracker-extension",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "eslint .",
    "test": "vitest run",
    "test:watch": "vitest",
    "check:manifest": "node scripts/validate-manifest.mjs",
    "check:secrets": "node scripts/scan-secrets.mjs",
    "ci": "npm run lint && npm run test && npm run check:manifest && npm run check:secrets"
  },
  "devDependencies": {
    "eslint": "^9.20.1",
    "vitest": "^2.1.8"
  }
}
